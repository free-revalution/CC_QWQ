# CC QwQ UI 改进设计文档

**日期**: 2025-02-02
**项目**: CC QwQ - 移动端远程操控 Claude Code
**设计目标**: 改进对话页面核心交互体验

---

## 一、消息显示体验改进

### 1.1 代码块显示组件

**设计目标**: 代码像在 IDE 中展示，而非普通文本。

**组件结构**:
- `CodeBlock` 组件包裹代码内容
- 顶部显示语言标签和文件名
- 行号显示（可选）
- 语法高亮（使用 Prism.js 或 Shiki）
- 一键复制按钮
- 深色背景，与普通消息区分

**样式规范**:
```css
背景: #1E1E1E (VS Code 深色主题)
文字: #D4D4D4
关键字: #569CD6 (蓝色)
字符串: #CE9178 (橙色)
注释: #6A9955 (绿色)
行号: #858585
圆角: 8px
内边距: 12px
字体: 'Fira Code' / 'JetBrains Mono' (等宽)
```

**消息类型区分**:
- **纯文本消息**: 白色玻璃卡片
- **代码块消息**: 深色代码容器，等宽字体
- **混合消息**: 文本 + 代码块组合

### 1.2 Claude 思考过程可视化

**展示方式**:
- 思考过程以"展开/收起"形式显示
- 默认收起，显示 "Thinking..." 动画
- 展开后显示完整思考过程
- 思考内容：斜体、灰色，与回复区分

**视觉设计**:
```
收起状态:
┌─────────────────────────┐
│ 🧔 Claude 正在思考...   │
│ ●●○ (脉冲动画)         │
└─────────────────────────┘

展开状态:
┌─────────────────────────┐
│ 🧔 思考过程 ▼          │
│ ────────────────────    │
│ <灰色斜体>             │
│ 让我分析一下这个需求... │
│                        │
│ 最终回复:              │
│ <正常文字>             │
└─────────────────────────┘
```

---

## 二、对话历史持久化方案

### 2.1 数据结构设计

**存储位置**（不使用后端）:
- **桌面端**: `localStorage` (Electron 也可用文件系统)
- **移动端**: `AsyncStorage` (React Native)

**数据模型**:
```typescript
interface Conversation {
  id: string           // 唯一 ID
  projectId: string    // 关联项目路径
  title: string        // 对话标题
  messages: Message[]  // 消息列表
  createdAt: number    // 创建时间
  updatedAt: number    // 最后更新时间
  isPinned: boolean    // 是否置顶
  tags: string[]       // 标签
}

interface StorageData {
  conversations: Conversation[]
  currentConversationId: string | null
  lastProjectPath: string | null
}
```

### 2.2 持久化实现

**桌面端**:
```typescript
const STORAGE_KEY = 'ccqwq_conversations'

function saveConversations(conversations: Conversation[]) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(conversations))
}

function loadConversations(): Conversation[] {
  const data = localStorage.getItem(STORAGE_KEY)
  return data ? JSON.parse(data) : []
}

function updateConversation(conversation: Conversation) {
  const list = loadConversations()
  const index = list.findIndex(c => c.id === conversation.id)
  if (index >= 0) {
    list[index] = conversation
  } else {
    list.unshift(conversation)
  }
  saveConversations(list)
}
```

**移动端**:
```typescript
import AsyncStorage from '@react-native-async-storage/async-storage'

async function saveConversations(conversations: Conversation[]) {
  await AsyncStorage.setItem('@ccqwq_conversations', JSON.stringify(conversations))
}
```

### 2.3 删除对话功能

**删除操作**:
- 单个删除：卡片删除按钮（悬浮显示）
- 批量删除：长按进入选择模式（移动端）
- 删除确认：弹窗确认

**删除实现**:
```typescript
function deleteConversation(conversationId: string) {
  const list = loadConversations()
  const filtered = list.filter(c => c.id !== conversationId)
  saveConversations(filtered)

  if (currentConversationId === conversationId) {
    setCurrentConversation(filtered[0]?.id || createNewConversation())
  }
}
```

### 2.4 数据同步（桌面端 ↔ 移动端）

**同步策略**:
1. 桌面端作为数据源
2. 移动端连接后请求历史
3. 新消息通过 WebSocket 实时同步
4. 移动端缓存用于离线查看

**WebSocket 协议**:
```typescript
// 请求历史
{ type: 'request_history' }

// 接收历史
{ type: 'history_data', data: { conversations: Conversation[] } }

// 同步消息
{ type: 'sync_message', data: { conversationId: string, message: Message } }

// 删除同步
{ type: 'delete_conversation', data: { conversationId: string } }
```

---

## 三、功能菜单交互设计

### 3.1 触发机制

**输入触发**（参考 VS Code /claude）:
- 输入 `#` → 弹出文件引用菜单
- 输入 `/` → 弹出命令菜单
- 菜单悬浮在输入框上方，左对齐
- 宽度与输入框一致

### 3.2 `#` 文件引用交互

**输入 `#` 后**:
```
输入: | #_|

弹出菜单:
┌─────────────────────────────────────────┐
│  🔍 搜索文件或文件夹...                 │
├─────────────────────────────────────────┤
│  📄 src/App.tsx          main.tsx       │
│  📄 src/index.css                      │
│  📁 components/           pages/         │
│  📄 package.json         tsconfig.json  │
├─────────────────────────────────────────┤
│  最近引用                                │
│    @src/utils/api.ts                   │
└─────────────────────────────────────────┘
```

**Tab 补全**:
```
输入: | #src/A|_|
     ↓ Tab
选中并补全
     ↓
结果: | #src/App.tsx|_|
```

**多选引用**:
```
输入: | #src/App.tsx #components/Button|_|
```

### 3.3 `/` 命令菜单交互

**输入 `/` 后**:
```
输入: | /_|

弹出菜单:
┌─────────────────────────────────────────┐
│  🔍 Filter commands...                  │
├─────────────────────────────────────────┤
│  Context                                │
│    /attach     Attach file...            │
│    /mention     Mention file from...     │
│    /clear       Clear conversation       │
├─────────────────────────────────────────┤
│  Model                                  │
│    /model       Switch model...          │
│    /thinking    Toggle thinking mode     │
│    /usage       Account & usage          │
├─────────────────────────────────────────┤
│  Slash Commands                         │
│    /help                                │
│    /commit                              │
│    /review                              │
├─────────────────────────────────────────┤
│  Skills                                 │
│    /ttd         Test-driven development  │
│    /refactor    Code refactoring        │
└─────────────────────────────────────────┘
```

**Tab 补全**:
```
输入: | /mod|_|
     ↓ Tab
补全: | /model|_| (显示参数提示)
```

### 3.4 智能提示和过滤

**实时过滤**:
- 输入 `/mo` → 过滤匹配项
- 高亮匹配字符
- 显示匹配数量

**模糊匹配**:
- `/usr` → 匹配 `/usage`
- `/cm` → 匹配 `/commit`

**历史记录**:
- 优先显示最近使用的命令
- 常用命令置顶

### 3.5 键盘快捷键

```
Tab         - 补全/下一项
Shift+Tab   - 上一项
Enter       - 确认
Esc         - 关闭
↑↓          - 导航
```

---

## 四、权限弹窗同步机制

### 4.1 弹窗设计

**视觉设计**（液态玻璃风格）:
```
┌─────────────────────────────────────────┐
│  🔔 需要您的确认                         │
├─────────────────────────────────────────┤
│  Claude 请求执行以下操作：                │
│  ┌─────────────────────────────────┐    │
│  │ 读取文件: src/App.tsx            │    │
│  │ 目的: 分析组件结构               │    │
│  └─────────────────────────────────┘    │
│  是否允许此操作？                         │
│  [  否  ]    [  是  ]                   │
└─────────────────────────────────────────┘
```

**按钮规范**:
- 主要操作（是/允许）：蓝色渐变，右侧
- 次要操作（否/拒绝）：玻璃按钮，左侧
- 危险操作：红色按钮
- 最小触摸区域：48×48px

### 4.2 同步协议

**桌面端 → 移动端**:
```typescript
{
  type: 'permission_request',
  data: {
    requestId: string,
    prompt: string,
    details: string,
    options?: {
      confirm: string,
      cancel: string
    }
  }
}
```

**移动端 → 桌面端**:
```typescript
{
  type: 'permission_response',
  data: {
    requestId: string,
    action: 'confirm' | 'cancel' | 'custom',
    value?: any
  }
}
```

**同步流程**:
```
1. Claude 需要权限
   ↓
2. 桌面端显示弹窗 + 发送到移动端
   ↓
3. 移动端显示同样弹窗
   ↓
4. 用户在任一端点击
   ↓
5. 两端关闭弹窗
   ↓
6. 返回结果给 Claude
```

### 4.3 超时处理

**超时机制**:
- 弹窗显示后 **60 秒**无响应自动超时
- 默认操作：`cancel`（拒绝）
- 显示超时提示

**超时协议**:
```typescript
{
  type: 'permission_timeout',
  data: {
    requestId: string,
    defaultAction: 'cancel'
  }
}
```

### 4.4 多选题弹窗

**数据结构**:
```typescript
{
  type: 'permission_request',
  data: {
    requestId: string,
    prompt: string,
    options: {
      type: 'single' | 'multiple',
      choices: [
        { id: 'fix-all', label: '全部修复' },
        { id: 'fix-critical', label: '仅修复关键错误' }
      ],
      default: 'fix-all'
    }
  }
}
```

### 4.5 弹窗队列

**队列管理**:
```typescript
interface PermissionQueue {
  pending: PermissionRequest[]    // 等待中
  current: PermissionRequest | null  // 当前显示
  history: PermissionResponse[]    // 历史记录
}
```

### 4.6 桌面端 vs 移动端

**桌面端特性**:
- 键盘快捷键（Y=Yes, N=No, Esc=Cancel）
- Tab 切换选项
- 最小化到托盘

**移动端特性**:
- 全屏模态弹窗
- 滑动关闭（仅取消）
- 震动反馈

**一致性保证**:
- 两端样式一致
- 按钮位置一致（确认在右）
- 颜色语义一致

---

## 五、实现优先级

### P0（核心功能）:
1. 代码块语法高亮显示
2. 对话历史 localStorage 持久化
3. `#` 文件引用菜单（带 Tab 补全）
4. 权限弹窗同步机制

### P1（重要功能）:
1. Claude 思考过程展示
2. `/` 命令菜单（带 Tab 补全）
3. 删除对话功能
4. 弹窗队列管理

### P2（增强功能）:
1. 多选题弹窗
2. 文件预览
3. 快捷键完整支持
4. 搜索和模糊匹配

---

## 六、技术栈建议

**桌面端**:
- 代码高亮: `Shiki` 或 `Prism.js`
- 状态管理: React Context / Zustand
- 持久化: `localStorage`

**移动端**:
- 持久化: `@react-native-async-storage/async-storage`
- 弹窗: `Modal` (React Native)

**通信**:
- WebSocket 消息扩展（新增类型）
