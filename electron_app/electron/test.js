// 简单的测试文件，用于测试模块导入
import { OperationLogger } from './operationLogger.js';

console.log('Successfully imported OperationLogger:', OperationLogger);

// 尝试创建一个实例
const logger = new OperationLogger();
console.log('Successfully created OperationLogger instance:', logger);
